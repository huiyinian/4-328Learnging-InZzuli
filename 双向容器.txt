#include<iostream>
#include<cstdio>
#include<algorithm>
#include<cstring>
#include<cmath>
#include<string>
#include<iterator>
#include<vector>
#include<stdlib.h>
#include<map>
#include<queue>
#include<stack>
#include<set>
#include<sstream>
typedef long long ll;
using namespace std;

void Egriiiiiiiii()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

map<int,bool>mp ;
int main()
{
    Egriiiiiiiii() ;
    int n, k ;
    cin>>n>>k ;
    int x;
    deque<int>dq;

    for(int i=0 ;i<n ;i++){
         cin>>x;
         if(!mp[x] && dq.size()==k){
              mp[x]=1 ;
              mp[dq.back()]=0;
              dq.pop_back() ;
              dq.push_front(x) ;
         }
        if(!mp[x] && dq.size()!=k){
              mp[x]=1 ;
              dq.push_front(x) ;
        }

    }
    cout<<dq.size()<<"\n" ;
    while(!dq.empty()){
        cout<<dq.front()<<" ";
        dq.pop_front() ;
    }
    return 0;
}
